web: bin/rails server -p 3000 -b 0.0.0.0
js: yarn build --watch
css: yarn build:css --watch

# Reccomend development runner with Docker
# You need PostgreSQL configurations.
# See also dotenv.example file.
#
# db: docker run --rm --name db --env-file .env --publish 5432:5432 --mount type=volume,src=sakazuki_db_storage,dst=/var/lib/postgresql/data postgres:13
# selenium: docker run --rm --name selenium -p 4444:4444 -p 7900:7900 --shm-size="512M" selenium/standalone-firefox:latest

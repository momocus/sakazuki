<!DOCTYPE html>
<html>
  <head>
    <title>SAKAZUKI</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= twitter_meta_tags %>

    <% if yield(:pack_style).empty? %>
      <%= stylesheet_pack_tag("common", media: "all", "data-turbolinks-track": "reload") %>
    <% else %>
      <%= yield(:pack_style) %>
    <% end %>
    <% if yield(:pack_js).empty? %>
      <%= javascript_pack_tag("common", "data-turbolinks-track": "reload") %>
    <% else %>
      <%= yield(:pack_js) %>
    <% end %>
  </head>

  <body>
    <%= render("layouts/header") %>
    <div class="container-xxl">
      <%= render("layouts/flashes") %>
      <%= yield %>
      <% if Rails.env.development? %>
        <%= tag.hr %>
        <%= debug(params) %>
      <% end %>
    </div>
  </body>
</html>
